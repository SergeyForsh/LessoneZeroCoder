import telebot

# Замените 'YOUR_TOKEN' на токен вашего бота
TOKEN = '7856763609:AAEpR_zzqDfWl0SM-zkVll1upBeYjyl4jS4'
bot = telebot.TeleBot(TOKEN)

@bot.message_handler(func=lambda message: True)
def echo_all(message):
    if message.text.lower() == "привет":
        bot.reply_to(message, "Привет! Как я могу помочь?")
    elif message.text.lower() == "как дела?":
        bot.reply_to(message, "Все отлично, спасибо! А у тебя как дела?")
    else:
        bot.reply_to(message, "Извини, я не понимаю. Попробуй спросить 'привет' или 'как дела?'.")

# Запуск бота
bot.polling()



### Как это работает:

# 1. **Импорт библиотеки**: Мы импортируем библиотеку `telebot`.
# 2. **Создание экземпляра бота**: Мы создаем экземпляр бота, передавая ему токен,
# полученный у [BotFather](https://t.me/botfather).
# 3. **Обработчик сообщений**: Используя декоратор `@bot.message_handler`,
# мы определяем функцию `echo_all`, которая отвечает на текстовые сообщения. Если сообщение "привет",
# бот отвечает "Привет! Как я могу помочь?". Если сообщение "как дела?",
# бот отвечает "Все отлично, спасибо! А у тебя как дела?". В противном случае бот отвечает,
# что не понимает вопрос.
# 4. **Запуск бота**: Метод `bot.polling()` запускает бота и начинает прослушивание входящих сообщений.
# ### Запуск бота
# 1. Замените `'YOUR_TOKEN'` на токен вашего бота, который вы можете получить у BotFather.
# 2. Сохраните код в файле, например, `my_bot.py`.
# 3. Запустите бота:
